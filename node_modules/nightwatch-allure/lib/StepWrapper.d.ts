/// <reference types="node" />
import { ContentType, Status } from "./allure/model";
import { AllureReporter } from "./AllureReporter";
import { StepInterface } from "./allure/Allure";
import { AllureStep } from "./allure/ExecutableItemWrapper";
export declare class StepWrapper {
    private readonly reporter;
    private readonly step;
    constructor(reporter: AllureReporter, step: AllureStep);
    startStep(name: string): StepWrapper;
    setStatus(status: Status): void;
    setDescription(description: string): void;
    setDetailsTrace(detailsTrace: string): void;
    setDetailsMessage(detailsMessage: string): void;
    addAttachment(name: string, content: Buffer | string, type: ContentType): void;
    endStep(timeMs?: number): void;
    run<T>(body: (step: StepInterface) => T): T;
}
